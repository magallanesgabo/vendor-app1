import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
        main: '#F1C232'
    },
    secondary: {
        main: '#0f0f0f'
    },
    tertiary: {
        main: '#fff'     
    },
  },
  
});

export default theme;